node_modules/
dist/
build/
.env
.env.local
*.log
logs/
packages/server/storage/
# STORAGE_LOCAL_PATH defaults to ./storage, which resolves against wherever
# the process was started. Run the tests from the repo root and the uploads
# land here instead.
/storage/
coverage/
.DS_Store
.vite/
*.tsbuildinfo
backups/

# Claude Code
.claude/settings.local.json

# Playwright
/test-results/
/playwright-report/
/blob-report/

# Python bytecode from the media worker
__pycache__/
*.pyc

# Linked WhatsApp sessions. These are the keys to somebody's WhatsApp account:
# anyone holding a copy can read and send as them. They live outside the repo by
# default (~/.ipropy-wa-sessions); this catches the case where WA_SESSION_DIR is
# pointed inside it.
wa-bridge/sessions/
.ipropy-wa-sessions/

# Editor temp files from network volumes (OneDrive/SMB leave these behind)
.!*

# Raw Vtiger export and the migration's checkpoint/load report — a full copy
# of a decade of business data, staged locally before it's mapped and loaded.
# Never belongs in git history.
packages/server/src/scripts/vtiger/.output/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Native app build output. The Android/iOS projects are committed; what Gradle
# and Xcode generate from them is not.
packages/app/android/build/
packages/app/android/app/build/
packages/app/android/.gradle/
packages/app/android/local.properties
packages/app/android/keystore.properties
packages/app/android/app/src/main/assets/public/
packages/app/ios/App/build/
packages/app/ios/App/Pods/
packages/app/ios/App/App/public/

# The app signing key. Losing it means no handset can ever take an update.
packages/app/android/ipropy-release.keystore
packages/app/android/*.keystore
