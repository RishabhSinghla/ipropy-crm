{
  "name": "iPropy \u2014 site visit to content",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "ipropy-shoot-finished",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "shoot-finished",
      "name": "Shoot finished",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        300
      ],
      "webhookId": "ipropy-shoot-finished"
    },
    {
      "parameters": {
        "jsCode": "// One normalised object for everything downstream, so a change to the CRM's\n// payload is a change in exactly one place.\nconst b = $json.body ?? $json;\n\nconst need = (k) => {\n  const v = b[k];\n  if (v === undefined || v === null || String(v).trim() === '') {\n    throw new Error(`The CRM did not send \"${k}\". Nothing can be filed without it.`);\n  }\n  return String(v).trim();\n};\n\nreturn [{\n  json: {\n    propertyId: need('propertyId'),\n    // Relative to the worker's root, e.g. \"GREENFIELD/B12-4BHK-250SQYD\".\n    folder: need('folder').replace(/^\\/+|\\/+$/g, ''),\n    sessionId: b.sessionId ?? null,\n    crmBaseUrl: (b.crmBaseUrl ?? 'http://localhost:4000').replace(/\\/+$/, ''),\n    startedAt: new Date().toISOString(),\n  },\n}];"
      },
      "id": "read-the-request",
      "name": "Read the request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        300
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $json.crmBaseUrl }}/api/records/properties/{{ $json.propertyId }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {
          "timeout": 30000
        }
      },
      "id": "get-property-from-crm",
      "name": "Get property from CRM",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        440,
        300
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "ipropy-crm",
          "name": "iPropy CRM API key"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://127.0.0.1:8712/prepare",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ folder: $('Read the request').item.json.folder }) }}",
        "options": {
          "timeout": 900000
        }
      },
      "id": "worker:-prepare",
      "name": "Worker: prepare",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        300
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "media-worker",
          "name": "iPropy media worker token"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// One item per photo, each carrying its small preview.\nconst res = $input.first().json;\nif (!res.previews || res.previews.length === 0) {\n  throw new Error(\n    `The worker found no usable photos in \"${res.folder}\". Either the upload ` +\n    `has not finished syncing, or \"01 Originals\" is empty.`\n  );\n}\nreturn res.previews.map((p) => ({ json: p }));"
      },
      "id": "split-previews",
      "name": "Split previews",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        880,
        300
      ]
    },
    {
      "parameters": {
        "batchSize": 1,
        "options": {}
      },
      "id": "each-photo",
      "name": "Each photo",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        1100,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\"model\": \"qwen/qwen3.7-flash\", \"temperature\": 0, \"response_format\": {\"type\": \"json_object\"}, \"messages\": [{\"role\": \"system\", \"content\": \"You label photographs of Indian builder-floor properties for an estate agency. JSON only, no prose: {\\\"room\\\":\\\"two words at most\\\",\\\"subject\\\":\\\"what it mainly shows, under 8 words\\\",\\\"broken\\\":true|false,\\\"why\\\":\\\"only if broken\\\"}. Use plain names a buyer would use: drawing room, kitchen, bedroom, bathroom, balcony, terrace, lobby, exterior, parking. Set broken=true ONLY for a photo that cannot be published at all \\u2014 badly out of focus, a shot of the floor or a shoe, a screenshot, a document, or a picture of a person. A merely ordinary photo is not broken.\"}, {\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"=Filename: {{ $json.master }}\"}, {\"type\": \"image_url\", \"image_url\": {\"url\": \"={{ $json.preview }}\"}}]}]}",
        "options": {
          "timeout": 120000
        }
      },
      "id": "look-at-the-photo",
      "name": "Look at the photo",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1320,
        480
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "openrouter",
          "name": "OpenRouter API key"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// A model that answers with junk must cost one label, never the run \u2014 so this\n// never throws. An unlabelled photo is still published, just as \"photo\".\nconst meta = $('Each photo').first().json;\nlet seen = { room: 'photo', subject: '', broken: false };\ntry {\n  seen = { ...seen, ...JSON.parse($json.choices[0].message.content) };\n} catch {\n  seen.subject = 'label unavailable';\n}\nreturn [{ json: { master: meta.master, ...seen } }];"
      },
      "id": "collect-label",
      "name": "Collect label",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        480
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// Order, do not cull.\n//\n// The instruction is explicit: the photos are taken carefully, so nothing gets\n// thrown away for being merely ordinary. The only things dropped are photos\n// the model called genuinely unpublishable \u2014 a shot of a shoe, a screenshot \u2014\n// and even those are recorded with the reason rather than vanishing.\n//\n// What this DOES decide is sequence, and that matters more than culling: the\n// photo sitting at position one on a 99acres listing is what decides whether\n// anybody clicks at all.\nconst all = $input.all().map((i) => i.json);\nconst broken = all.filter((p) => p.broken);\nconst usable = all.filter((p) => !p.broken);\n\n// The order a buyer wants to walk the property in.\nconst ROOM_ORDER = [\n  'drawing room', 'living room', 'lobby', 'kitchen', 'dining',\n  'bedroom', 'master bedroom', 'bathroom', 'balcony', 'terrace',\n  'exterior', 'parking', 'staircase',\n];\nconst rank = (room) => {\n  const r = (room || '').toLowerCase();\n  const i = ROOM_ORDER.findIndex((k) => r.includes(k) || k.includes(r));\n  return i === -1 ? ROOM_ORDER.length : i;\n};\n\n// One of each room first, in walking order, then the remaining angles behind\n// them in the same order. Eight views of one drawing room up front makes a\n// good property look thin; the same eight further down are just detail.\nconst firstOfRoom = [];\nconst rest = [];\nconst seenRoom = new Set();\nfor (const p of [...usable].sort((a, b) => rank(a.room) - rank(b.room))) {\n  const key = (p.room || 'photo').toLowerCase();\n  (seenRoom.has(key) ? rest : firstOfRoom).push(p);\n  seenRoom.add(key);\n}\n\nconst plan = [...firstOfRoom, ...rest].map((p, i) => ({\n  master: p.master,\n  label: p.room || 'photo',\n  order: i + 1,\n  subject: p.subject,\n}));\n\nreturn [{ json: {\n  plan,\n  rooms: [...seenRoom],\n  total: all.length,\n  publishing: plan.length,\n  dropped: broken.map((p) => ({ master: p.master, why: p.why || 'unpublishable' })),\n} }];"
      },
      "id": "put-them-in-order",
      "name": "Put them in order",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1320,
        120
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\"model\": \"qwen/qwen3.7-flash\", \"temperature\": 0.4, \"response_format\": {\"type\": \"json_object\"}, \"messages\": [{\"role\": \"system\", \"content\": \"You write listing copy for iPropy, an estate agency in Faridabad, Haryana. Plain Indian English. No emoji walls, no \\\"DM for price\\\", and never a fact you were not given \\u2014 if a number is not in the data, do not state it. Prices in lakh and crore as Indians write them. JSON only: {\\\"instagram\\\":\\\"...\\\",\\\"facebook\\\":\\\"...\\\",\\\"whatsapp\\\":\\\"...\\\",\\\"status_line\\\":\\\"...\\\",\\\"google\\\":\\\"...\\\",\\\"shorts_title\\\":\\\"...\\\",\\\"portal_title\\\":\\\"...\\\",\\\"portal_description\\\":\\\"...\\\",\\\"hashtags\\\":[\\\"...\\\"]}. whatsapp must read like a message from a person, under 400 characters. status_line is one short line. shorts_title is under 90 characters. portal_description is 4 to 6 sentences.\"}, {\"role\": \"user\", \"content\": \"=Property facts as JSON:\\n{{ JSON.stringify($('Get property from CRM').item.json) }}\\n\\nRooms photographed: {{ $json.rooms.join(\\\", \\\") }}\\nPhotos being published: {{ $json.publishing }}\"}]}",
        "options": {
          "timeout": 180000
        }
      },
      "id": "write-the-words",
      "name": "Write the words",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1540,
        120
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "openrouter",
          "name": "OpenRouter API key"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// The three files a person actually opens, plus the plan the worker applies.\nconst cfg      = $('Read the request').first().json;\nconst ordered  = $('Put them in order').first().json;\nconst property = $('Get property from CRM').first().json;\n\nlet copy = {};\ntry { copy = JSON.parse($json.choices[0].message.content); } catch { copy = {}; }\nconst line = (s) => (s ?? '').toString().trim();\nconst tags = (copy.hashtags ?? []).map((h) => (h.startsWith('#') ? h : '#' + h)).join(' ');\nconst name = property.label ?? cfg.propertyId;\n\nconst captions = [\n  `# Captions \u2014 ${name}`,\n  ``,\n  `Written ${new Date().toISOString().slice(0, 16).replace('T', ' ')} UTC. Read before posting.`,\n  ``,\n  `## Instagram (feed + reel)`, ``, line(copy.instagram), ``, tags, ``,\n  `## Facebook (page, groups, marketplace)`, ``, line(copy.facebook), ``,\n  `## WhatsApp \u2014 send to a buyer`, ``, line(copy.whatsapp), ``,\n  `## WhatsApp Status / Instagram Story`, ``, line(copy.status_line), ``,\n  `## Google Business update`, ``, line(copy.google), ``,\n  `## YouTube Shorts title`, ``, line(copy.shorts_title), ``,\n].join('\\n');\n\nconst listing = [\n  `# Portal listing \u2014 ${name}`,\n  ``,\n  `For 99acres, MagicBricks, Housing, NoBroker and Facebook Marketplace.`,\n  `Photos are in \"03 Portals and Website\", already in this order.`,\n  ``,\n  `**Title**`, ``, line(copy.portal_title), ``,\n  `**Description**`, ``, line(copy.portal_description), ``,\n  `---`, ``, `## Photo order`,\n  ...ordered.plan.map((p) => `${p.order}. ${p.label}${p.subject ? ' \u2014 ' + p.subject : ''}`),\n].join('\\n');\n\nconst status = {\n  ok: true,\n  finished_at: new Date().toISOString(),\n  started_at: cfg.startedAt,\n  property_id: cfg.propertyId,\n  folder: cfg.folder,\n  model: 'qwen/qwen3.7-flash',\n  photos_seen: ordered.total,\n  photos_published: ordered.publishing,\n  rooms: ordered.rooms,\n  // Recorded, not hidden: a photo that disappeared without explanation is the\n  // thing that makes people stop trusting the whole pipeline.\n  not_published: ordered.dropped,\n};\n\nreturn [{ json: {\n  plan: ordered.plan,\n  files: {\n    'captions.md': captions,\n    'listing.md': listing,\n    '_status.json': JSON.stringify(status, null, 2),\n  },\n  summary: `${ordered.publishing} photos published across 4 folders`,\n} }];"
      },
      "id": "build-the-files",
      "name": "Build the files",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        120
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://127.0.0.1:8712/finish",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({folder: $('Read the request').item.json.folder,plan: $json.plan,files: $json.files}) }}",
        "options": {
          "timeout": 900000
        }
      },
      "id": "worker:-publish",
      "name": "Worker: publish",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1980,
        120
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "media-worker",
          "name": "iPropy media worker token"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Read the request').item.json.crmBaseUrl }}/api/webhooks/n8n/content-ready",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({propertyId: $('Read the request').item.json.propertyId,sessionId: $('Read the request').item.json.sessionId,folder: $('Read the request').item.json.folder,summary: $('Build the files').item.json.summary}) }}",
        "options": {
          "timeout": 30000
        }
      },
      "id": "tell-the-crm",
      "name": "Tell the CRM",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2200,
        120
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "crm-callback",
          "name": "iPropy CRM callback secret"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ ok: true, summary: $('Build the files').item.json.summary }) }}",
        "options": {}
      },
      "id": "answer-the-crm",
      "name": "Answer the CRM",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        2420,
        120
      ]
    }
  ],
  "connections": {
    "Shoot finished": {
      "main": [
        [
          {
            "node": "Read the request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read the request": {
      "main": [
        [
          {
            "node": "Get property from CRM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get property from CRM": {
      "main": [
        [
          {
            "node": "Worker: prepare",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Worker: prepare": {
      "main": [
        [
          {
            "node": "Split previews",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Split previews": {
      "main": [
        [
          {
            "node": "Each photo",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Each photo": {
      "main": [
        [
          {
            "node": "Put them in order",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Look at the photo",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Look at the photo": {
      "main": [
        [
          {
            "node": "Collect label",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Collect label": {
      "main": [
        [
          {
            "node": "Each photo",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Put them in order": {
      "main": [
        [
          {
            "node": "Write the words",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write the words": {
      "main": [
        [
          {
            "node": "Build the files",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build the files": {
      "main": [
        [
          {
            "node": "Worker: publish",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Worker: publish": {
      "main": [
        [
          {
            "node": "Tell the CRM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Tell the CRM": {
      "main": [
        [
          {
            "node": "Answer the CRM",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true
  },
  "pinData": {},
  "tags": []
}
